# Server Configuration
server_port: 8080
log_level: info  # Options: debug, info, warn, error

# Database Configuration
postgres_dsn: postgres://user:password@localhost:5432/quizapp_db?sslmode=disable

# Redis Configuration
redis_addr: localhost:6379
redis_password: ""  # Optional

# Firebase Configuration
firebase_project_id: sudal-14497
firebase_credentials_json: ./secrets/firebase_admin_key.json

# JWT Configuration
jwt_secret_key: your-jwt-secret-key

# Application Settings
# app_env controls environment: dev | canary | production | test
# Precedence: ENV vars > .env > this file
app_env: dev
